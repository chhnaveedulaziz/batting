# frozen_string_literal: true

RSpec.describe BattingAvg do
  it "has a version number" do
    expect(BattingAvg::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
